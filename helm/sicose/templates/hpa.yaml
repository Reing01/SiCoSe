{{- if .Values.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "sicose.fullname" . }}-backend
  namespace: {{ include "sicose.namespace" . }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "sicose.fullname" . }}-backend
  minReplicas: {{ default (include "sicose.targetReplicas" .) .Values.autoscaling.minReplicas }}
  maxReplicas: {{ default (include "sicose.targetReplicas" .) .Values.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.autoscaling.targetCPUUtilizationPercentage }}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "sicose.fullname" . }}-frontend
  namespace: {{ include "sicose.namespace" . }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "sicose.fullname" . }}-frontend
  minReplicas: {{ default (include "sicose.targetReplicas" .) .Values.autoscaling.minReplicas }}
  maxReplicas: {{ default (include "sicose.targetReplicas" .) .Values.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
